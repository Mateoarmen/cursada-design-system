{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/mateoarmendariz/Documents/CLAUDE/cursada-design-system && jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */src/*.html|*/src/*.css|*/src/*.js) npm run build:app 2>&1 | tail -5 ;; *) true ;; esac; }",
            "timeout": 30,
            "statusMessage": "Building Cursada..."
          }
        ]
      }
    ]
  }
}
